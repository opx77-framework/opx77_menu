# Build and local tooling
dist/
*.luac
node_modules/

# Editors
.idea/
*.swp
.DS_Store

# Local server configuration: contains credentials
server.jsonc
acl.jsonc
.env

# Agent tooling: 16 MB of skills, two of them nested git repos
.claude/

# ── Dev only, never shipped and never pushed ────────────────────────────────
#
# preview.html is GENERATED by tools/build-preview.py, weighs half a megabyte
# because the still is embedded as a data URI, and would land in a diff on every
# regeneration. The generator is what belongs in the repository; the output is
# one command away.
preview.html

# The still it embeds is somebody else's screenshot. Point the generator at
# your own by dropping a JPEG here under the same name.
tools/

# next person regenerates the preview and proves the resource still behaves.
